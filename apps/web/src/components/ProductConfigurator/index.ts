/**
 * ProductConfigurator -- Barrel Export
 * =============================================================================
 *
 * Re-exports the public API of the ProductConfigurator component module.
 * Consuming code should import from this barrel rather than from individual
 * files to maintain stable import paths.
 *
 * =============================================================================
 */

export { ProductConfiguratorPage } from './ProductConfiguratorPage';
export type { ConfiguratorStepId, ConfiguratorSelections, SummaryTabId } from './types';
