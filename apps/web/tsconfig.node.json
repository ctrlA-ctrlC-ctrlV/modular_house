{
  /**
   * Node-Side TypeScript Configuration
   * =========================================================================
   * Covers Vite configuration and build scripts that execute in a Node.js
   * environment rather than the browser.
   *
   * src/routes-metadata.ts and src/types/seo.ts are included explicitly
   * because the prerender and sitemap scripts import them. These files also
   * appear in tsconfig.json's "include": ["src"], which is intentional;
   * the typecheck script runs both configs independently with --noEmit.
   * =========================================================================
   */
  "compilerOptions": {
    "composite": true,
    "skipLibCheck": true,
    "module": "ESNext",
    "moduleResolution": "bundler",
    "allowSyntheticDefaultImports": true,
    "strict": true,
    "types": ["node"]
  },
  "include": [
    "vite.config.ts",
    "scripts",
    "src/routes-metadata.ts",
    "src/types/seo.ts"
  ]
}