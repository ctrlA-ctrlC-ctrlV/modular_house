# Test Environment Configuration
NODE_ENV=test

# Test Database (using local Docker container)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/test_db

# Test API Configuration
PORT=8081
CORS_ORIGIN=http://localhost:3000

# Test Email (MailHog or mock)
MAIL_HOST=localhost
MAIL_PORT=1025
MAIL_SECURE=false
MAIL_USER=
MAIL_PASS=
MAIL_FROM_NAME=Modular House Test
MAIL_FROM_EMAIL=test@modular.house
MAIL_INTERNAL_TO=admin@modular.house

# Test Security
JWT_SECRET=test-jwt-secret-change-for-production-tests
PASSWORD_SALT_ROUNDS=4

# Test Customer Confirmation
CUSTOMER_CONFIRM_ENABLED=false
